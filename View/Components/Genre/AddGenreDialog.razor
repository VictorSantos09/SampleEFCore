@inject IDialogService _dialogService
@inject GenreService _service
@inject NavigationManager _navManager
@using System.ComponentModel.DataAnnotations

<MudDialog>
    <DialogContent>
        <MudPaper Class="pa-4">
            <MudForm @ref="_form" @bind-IsValid="@_success">
                <MudTextField T="string" Label="Nome" Required="true" @bind-Value="GenreName"
                              Counter="45" MaxLength="45" Immediate="true"
                              Validation="@(new Func<string, IEnumerable<string>>(GenreValidator.MaxDigitsName))" RequiredError="Nome do gênero é necessário" />
            </MudForm>
        </MudPaper>
        <MudPaper Class="d-block pa-4 mt-4">
            <MudButton Variant="Variant.Filled" Color="Color.Primary" DisableElevation="true" OnClick="@(() => Submit())">Salvar</MudButton>
            <MudButton Variant="Variant.Filled" Color="Color.Secondary" DisableElevation="true" OnClick="@(() => _form.ResetAsync())" Class="mx-2">Apagar Dados</MudButton>
        </MudPaper>
    </DialogContent>
</MudDialog>

@code {
    private bool _success;
    private MudForm _form;
    private string GenreName;

    private async Task Submit()
    {
        await _form.Validate();
        if (_form.IsValid)
        {
            GenreModel genre = new GenreModel() { Name = GenreName };
            await _service.CreateAsync(genre);
            _navManager.NavigateTo(_navManager.Uri, true);
        }
    }
}
